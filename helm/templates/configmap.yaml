apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kubewatcher.fullname" . }}
  labels:
  {{- include "kubewatcher.labels" . | nindent 4 }}
data:
  config.yaml: |-
    {{- toYaml .Values.config | nindent 4 }}
